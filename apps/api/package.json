{
  "name": "@glia/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn glia.main:app --reload --port 8000",
    "lint": "uv run ruff check .",
    "typecheck": "uv run mypy .",
    "test": "uv run pytest"
  }
}
